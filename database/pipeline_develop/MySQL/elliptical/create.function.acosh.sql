DROP FUNCTION IF EXISTS acosh;

DELIMITER //

CREATE FUNCTION acosh(x DOUBLE) RETURNS DOUBLE 
DETERMINISTIC 
BEGIN

  IF x >= 1 THEN
    RETURN LN(x + SQRT(x * x - 1));
  ELSE
    RETURN NULL;
  END IF;

END;
//

DELIMITER ;
